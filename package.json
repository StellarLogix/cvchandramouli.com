{
  "name": "cvchandramouli-website",
  "version": "1.0.0",
  "homepage": "https://stellarlogix.github.io/cvchandramouli.com",
  "description": "C.V. Chandramouli Advertising - Professional marketing and advertising services",
  "main": "index.js",
  "scripts": {
    "dev": "react-scripts start",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -b gh-pages",
    "deploy:clean": "gh-pages -d build -b gh-pages --dotfiles --git git"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "gh-pages": "^6.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
